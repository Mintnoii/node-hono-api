{
  "name": "node-hono-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev:be": "pnpm --filter=backend run dev",
    "migrate": "pnpm --filter=backend run migrate"
  },
  "devDependencies": {
    "@types/node": "^20.12.8",
    "typescript": "^5.5.4"
  }
}
